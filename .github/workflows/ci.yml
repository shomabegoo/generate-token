name: CI
'on':
  workflow_dispatch: {}
  release:
    types:
    - published
  push:
    branches:
    - main
  pull_request: {}
  create: {}
jobs:
  generate-token:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        fetch-depth: '0'

    - name: my-step
      id: my-step
      run: |
        echo "::set-output name=my_output::Hello, world!"
        output=$(.github/workflows/echo.sh)
        echo "my_second_output=$output" >> $GITHUB_OUTPUT
    
    - name: print-output
      run: |
        echo "Output of my-step: ${{ steps.my-step.outputs.my_second_output }}"

    - id: foo
      run: echo "bar=5" >> $GITHUB_OUTPUT
    - run: echo "bar= ${{steps.outputs.foo.bar}}"
    - name: Get Token
      id: get-token
      run: |
        token=$(.github/workflows/get-github-app-token.sh)
        echo "my_token=$token" >> $GITHUB_OUTPUT
      env:
        APP_SECRET: ${{ secrets.APP_SECRET }}
        APP_ID: ${{ secrets.APP_ID }}
        GH_REPO: ${{ github.repository }}
    - name: print token
      run: |
        echo "token: ${{ steps.outputs.get-token.my_token }}"
    - name: Publish example file
      run: |
        echo "This is an example file." > example.txt
        git config --global user.email "actions@github.com"
        git config --global user.name "GitHub Actions"
        git checkout -b my-feature-branch
        git add example.txt
        git commit -m "Add example file"
        git push origin my-feature-branch --force
      env:
        GITHUB_TOKEN: ${{ steps.get-token.outputs.my_token }}
    - name: Create PR
      run: |
        gh pr create \
          --title "New feature" \
          --body "This pull request adds a new feature" \
          --base main \
          --head my-feature-branch
      env:
        GITHUB_TOKEN: ${{ steps.get-token.outputs.my_token }}